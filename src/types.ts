export interface Game {
  id: number;
  name: string;
  cover: string;
  url: string;
}

export interface Theme {
  name: string;
  bg: string;
  accent: string;
  blue: string;
  text: string;
  surface: string;
  border: string;
  cardBg: string;
  primary: string;
}
